{
  "name": "tracker-app",
  "version": "1.0.0",
  "private": true,
  "description": "Persönlicher Tracker (PWA), für iOS via Capacitor verpackt.",
  "type": "module",
  "scripts": {
    "copy:web": "node scripts/copy-web.mjs",
    "sync": "npm run copy:web && npx cap sync",
    "add:ios": "npm run copy:web && npx cap add ios",
    "open:ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
